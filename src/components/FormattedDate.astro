---
interface Props {
	date: import('../utils/dates').ContentDateValue;
	showTime?: boolean;
}

const { date, showTime = false } = Astro.props;
const { formatFullDate } = await import('../utils/dates');
---

<time datetime={date.value.toISOString()}>{formatFullDate(date, showTime)}</time>
